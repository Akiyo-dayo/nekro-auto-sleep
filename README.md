# NekroAgent 自动睡眠插件

为每个会话提供独立的拟人化睡眠周期，无需修改 NekroAgent 核心代码。
同时兼容 Akiyo 版 (`Akiyo-dayo/NekroAgent_ByAkiyo`) 与原版上游 (`KroMiose/nekro-agent`)。

## 功能

- **定时入睡与随机起床**：默认每晚 23:00 静默入睡，次日 06:45–08:30 之间随机自然醒
- **由 LLM 判断的叫醒协议**：呼叫→固定提示（不经 LLM）→**第二次触发直接进 LLM**，
  由它读懂对方到底要不要叫醒：要就正常回复，不要就静默睡回去。
  插件不做关键词猜测；每夜提示次数与冷却上限，防止凌晨被刷屏
- **唤醒后上下文连贯**：整夜消息照常入库，叫醒提示进历史，被叫醒后每一轮都能拿到
  「几点就寝、被谁叫醒、醒了多久、夜里的消息是睡着时收到的」
- **夜间定时任务照跑**：用户排的定时提醒在睡眠期间仍会执行，Bot 知道自己是「夜里值班」，
  处理完继续睡；值班时长按半睡计入质量。也可整夜屏蔽
- **重启自愈**：启动时对账所有频道，补结算错过的自然醒、补入睡错过的夜晚
- **主动睡下**：Bot 可调用 `resume_sleep` 工具重新入睡
- **空闲自动睡回**：被叫醒后无新互动自动静默睡回（默认 10 分钟）
- **可解释的睡眠质量**：100% = 睡满目标时长且没被打扰，睡得更久会超过 100%；
  每一项扣分都会随结算一起存下来备查
- **自然醒播报**：默认每次自然醒都播报，可切换为「仅当夜里被打扰过」或「从不」
- **按频道 / 按人设的作息**：同一份全局配置之上，可以只把某个群或某个人设的作息挪走
- **运维指令**：`/sleep status` 直出昨夜评分的每一项，`/sleep now|wake|skip` 手动介入
- **拟人化细节**：睡前预告、起床后赖床、起床说梦话、周末晚睡（都可单独开关）

## 安装

把 **`nekro_auto_sleep/`** 这个目录（不是仓库根目录）复制到 NekroAgent 的 `plugins/external/` 下：

```
plugins/external/nekro_auto_sleep/
├── __init__.py
├── models.py
├── schedule.py
├── quality.py
├── engine.py
├── persistence.py
└── runtime.py
```

重启 NekroAgent 后在插件管理页面启用「自动睡眠」插件。

## 从 v1 升级

插件配置是**按字段持久化**的，所以升级后新的默认值只对全新安装生效，老装机保留原值。
有一项必须手动改：

| 配置项 | v1 默认 | v2 默认 | 不改会怎样 |
|---|---|---|---|
| 睡眠质量下限 | `60` | `20` | v2 的量程是 20–110，下限 60 会把所有糟糕的夜晚一律夹到 60，**看起来就像分数不动**——正是 v1 那个 bug 的症状 |
| 质量稳定扰动幅度 | `4.0` | `2.0` | 分数现在本就随实际情况浮动，4.0 的随机量偏大 |

插件启动时会对下限过高发一条 WARNING，`/sleep status` 也会把它显示出来，
并在分数真的被夹住时标注「原始分 X，被下限 Y 夹住」。

已删除的配置项（`NEAR_WAKE_RATIO`、`AFFIRMATIVE_KEYWORDS`、`NEGATIVE_KEYWORDS`、
`UNCLEAR_ANSWER`、`TIMER_AGENT_WAIT_TIMEOUT_SECONDS`）会在下次保存配置时自动消失，不用手动清。

## 配置

所有配置项均支持 WebUI 中文界面。下表由 `tools/gen_config_table.py` 从配置类生成，
`tests/test_docs.py` 会在它与代码漂开时报错。

<!-- config-table:begin -->
| 配置项 | 键名 | 默认值 | 说明 |
|---|---|---|---|
| 启用自动睡眠 | `ENABLED` | `true` | 总开关，关闭后所有会话停止新的睡眠行为 |
| 时区 | `TIMEZONE` | `Asia/Shanghai` | IANA 时区名称，所有睡眠时间按此时区解释 |
| 入睡时间 | `SLEEP_TIME` | `23:00` | 每日自动入睡的时间，格式 HH:MM |
| 起床时间范围（起始） | `WAKE_TIME_START` | `06:45` | 随机起床时间的最早时刻，格式 HH:MM |
| 起床时间范围（结束） | `WAKE_TIME_END` | `08:30` | 随机起床时间的最晚时刻，格式 HH:MM |
| 起床随机粒度（分钟） | `WAKE_RANDOM_STEP_MINUTES` | `1` | 在起床范围内按此分钟数生成候选起床点，1-60 |
| 接近起床提前量（分钟） | `NEAR_WAKE_MINUTES` | `60` | 距离计划起床还有这么多分钟时，提示语改为「还没起床」，0-720 |
| 叫醒确认窗口（秒） | `WAKE_CONFIRM_WINDOW_SECONDS` | `180` | 发出「要叫醒吗」之后，多久之内的回答还算数。超时则问题作废，下次呼叫重新问，10-1800 |
| 紧急唤醒关键词 | `URGENT_KEYWORDS` | 空 | 可选的关键词直通：消息里出现这些词且是冲着 Bot 来的，就跳过「要叫醒吗」直接叫醒。默认留空关闭——是否需要叫醒本来是交给 LLM 判断的，关键词只会带来误判 |
| 叫醒提示语 | `WAKE_PROMPT_ASLEEP` | `【{persona}已经睡了 要叫醒{persona}吗？】` | 睡眠中被呼叫时发出的固定提示（不经 LLM），{persona} 会替换为人设名。想让用户知道怎么回可以写成「…要叫醒吗？（回「叫醒」或「不用」）」 |
| 接近起床时的提示语 | `WAKE_PROMPT_NEAR_WAKE` | `【{persona}还没起床 要叫醒{persona}吗？】` | 在「接近起床提前量」内被呼叫时改用这条，{persona} 会替换为人设名 |
| 谁的回答算数 | `ANSWER_SCOPE` | `offeree` | offeree: 只有被问的那个人的回答算数；anyone: 群里任何人都能回答 |
| 每夜最多提示次数 | `MAX_OFFERS_PER_NIGHT` | `3` | 同一晚最多发送几次「要叫醒吗」，超出后静默，1-20 |
| 提示冷却（分钟） | `OFFER_COOLDOWN_MINUTES` | `20` | 两次「要叫醒吗」之间的最短间隔，0-240 |
| 拒绝后静默时长（分钟） | `SNOOZE_MINUTES` | `30` | 有人明确说不用叫醒后，这段时间内不再提示，0-480 |
| 睡前预告提前量（分钟） | `BEDTIME_NOTICE_MINUTES` | `0` | 入睡前这么多分钟先说一句「有点困了」，0 表示不预告，0-120 |
| 睡前预告文案 | `BEDTIME_NOTICE_TEMPLATE` | `【{persona}有点困了，准备去睡了…】` | {persona} 会替换为人设名。这条不经 LLM |
| 睡前预告静默门槛（小时） | `BEDTIME_NOTICE_QUIET_HOURS` | `24` | 距上一条用户消息超过这么久的频道不预告，免得对着没人的群自言自语，1-720 |
| 赖床时长（分钟） | `GROGGY_MINUTES` | `30` | 自然醒之后这段时间里，提示词会告诉 Bot 它刚起床、还迷糊着，0 表示关闭，0-240 |
| 起床后说梦话 | `DREAM_LINE` | `false` | 自然醒播报之后，再让 LLM 顺口说一句昨晚梦到了什么。**会多跑一轮 LLM**，默认关闭 |
| 梦话提示词 | `DREAM_PROMPT` | `你刚刚自然醒来。可以顺口提一句昨晚梦到了什么，一句话就好，不要长篇大论，也不用解释自己在做梦。` | 开启说梦话后注入给 LLM 的系统提示 |
| 周末作息适用星期 | `WEEKEND_DAYS` | 空 | 以**就寝当天**计，0=周一…6=周日；填 4,5 表示周五、周六晚上用周末作息。留空表示不区分 |
| 周末入睡时间 | `WEEKEND_SLEEP_TIME` | 空 | 留空则沿用平日的入睡时间，格式 HH:MM |
| 周末起床范围（起始） | `WEEKEND_WAKE_TIME_START` | 空 | 留空则沿用平日，格式 HH:MM |
| 周末起床范围（结束） | `WEEKEND_WAKE_TIME_END` | 空 | 留空则沿用平日，格式 HH:MM |
| 历史记录模式 | `HISTORY_MODE` | `preserve` | preserve: 首次叫醒消息保留在历史中；strict: 完全拦截不记录 |
| 呼叫关键词 | `CALL_KEYWORDS` | `醒醒,起床,在吗` | 触发叫醒的关键词，逗号或换行分隔 |
| 默认人格名 | `FALLBACK_PERSONA_NAME` | `Bot` | 无法获取当前预设名称时使用的回退名 |
| 提前叫醒空闲超时（分钟） | `EARLY_WAKE_IDLE_MINUTES` | `10` | 被叫醒后无新互动的自动睡回时间，1-240 |
| 离线补发宽限期（分钟） | `WAKE_NOTICE_GRACE_MINUTES` | `120` | 重启后补发自然醒通知的最大延迟，超过则静默结算，0-1440 |
| 起床播报策略 | `WAKE_NOTICE_POLICY` | `always` | always: 自然醒即播报；if_disturbed: 仅当夜里有人叫过时播报；never: 从不播报 |
| 启动装载活跃天数 | `HYDRATE_ACTIVE_DAYS` | `14` | 启动时装载最近多少天内有消息的频道，装载后它们才会按时入睡，1-365 |
| 维护循环间隔（秒） | `MAINTENANCE_INTERVAL_SECONDS` | `15` | 后台维护任务的检查间隔，2-300 |
| 夜间定时任务策略 | `NIGHT_TIMER_POLICY` | `run` | run（默认）：定时提醒照常执行，Bot 知道自己是在夜里值班，值班时长按半睡计入睡眠质量；block：夜间定时提醒只入历史、不执行 |
| 夜间值班计入时长（分钟） | `NIGHT_DUTY_ASSUMED_MINUTES` | `3` | 每次夜间定时任务按这么久计入睡眠质量（其中一半算作损失的休息），0-120。插件拿不到轮次的真实耗时，取一个保守估计而不是去猴补丁宿主内部方法 |
| 目标睡眠时长（小时） | `SLEEP_TARGET_HOURS` | `0.0` | 固定的 100% 基准时长。填 0（默认）表示以当晚自己的计划睡眠时长为准——起床时间本来就是范围内随机的，拿固定值当基准会让「今天起得早」被当成睡得差，0-16 |
| 睡眠质量下限 | `QUALITY_MIN` | `20` | 睡眠质量百分比的最低值，0-100 |
| 睡眠质量上限 | `QUALITY_MAX` | `120` | 睡眠质量百分比的最高值，100-200，不小于下限 |
| 质量稳定扰动幅度 | `QUALITY_JITTER_POINTS` | `2.0` | 每次睡眠质量的随机扰动范围（正负），0-15 |
<!-- config-table:end -->

## 运维指令

两种写法都行：`/sleep status` 与 `/sleep.status`。宿主按第一个空格切命令名，
命令组注册出来的是展平的 `sleep.status`，所以插件另外注册了一个 `sleep` 顶层命令
把空格写法转发过去——否则 `/sleep status` 会解析不到命令、静默无响应。

| 指令 | 权限 | 说明 |
|---|---|---|
| `/sleep status` | 高级用户 | 当前状态、今夜提示次数、**上一夜评分的每一项** |
| `/sleep now` | 超级用户 | 立刻入睡（从此刻开始计时，不是从配置的就寝点） |
| `/sleep wake` | 超级用户 | 立刻起床并结算，会照常播报 |
| `/sleep skip` | 超级用户 | 今晚不睡，明晚恢复 |
| `/sleep set <字段> <值> [scope]` | 超级用户 | 设置作息覆盖，`scope` 为 `channel`（默认）或 `preset` |
| `/sleep unset [scope]` | 超级用户 | 清除覆盖 |

`set` 的字段：`tz`（IANA 时区）、`bed`（就寝 HH:MM）、`wake-start`、`wake-end`。
值会先校验再落库，写错时间或时区会直接报错而不是留到半夜才发作。

`/sleep status` 的样子：

```
【阿绫 · 睡眠状态】
作息：23:00 → 06:45~08:30（Asia/Tokyo）　覆盖：timezone=本频道
当前：睡着（23:00 就寝，计划 07:24 起床，已睡 4 小时 12 分钟）
今夜：已提示 1/3 次，静默至 02:47
上一夜：103%，睡了 8 小时 41 分钟
　　基准 8.68h · 覆盖 100.0 · 碎片 -0.0 · 呼叫 -0.0 · 叫醒 -0.0 · 整夜无扰 +3.0 · 扰动 +0.3
本夜记录：没理会的呼叫 1 次，真被叫醒 0 次
```

## 作息覆盖

优先级 **本频道 > 人设 > 实例 > 全局配置**，逐字段生效——只改就寝时间不会连时区一起钉死。

- **按频道**：跨时区的群不用再跟着全局时区睡觉
- **按人设**：换人设就换作息，和这个项目里记忆/好感按人设隔离是同一套思路
- **按实例**（仅 Akiyo fork）：多账号部署时每个实例可以有自己的作息

`/sleep status` 会标出每个字段是从哪一层来的。频道与人设两级存在插件自己的存储里（不建表），
实例级读的是宿主的实例配置覆盖。

### 多实例共群

同一个群里的两个账号会互相看见对方发的消息。叫醒提示里含人设名，而人设名是「有效呼叫」的判据之一，
所以**一个 Bot 的「要叫醒 X 吗？」对另一个 Bot 来说就是一次呼叫**——不挡住的话两边会互相搭话，
直到当晚提示次数用完，还白白扣掉睡眠质量分。

插件会读 `DBAdapterInstance.provider_account_id` 认出本部署自己的所有账号，
来自这些账号的消息**照常入历史，但不参与叫醒协议**：不建 offer、不算回答、也不刷新赖床倒计时。
（这是 v2 第一晚在测试机上实际观察到的。）

fork 的 `chat_key` 自带实例段（`onebot_v11-inst1-group_123456`），所以同一个群里的两个账号
天生是两条独立的记录：**各自的睡眠状态、各自随机抽的起床时间、各自的质量评分**。
起床点的随机种子取自 `chat_key`，两个实例即使配置完全相同也会抽到不同的分钟数。

但有一处以前是被拍平的：`ScopedPluginConfig` 靠适配器在入站时写的 contextvar 解析实例级配置，
而**维护循环是后台任务、没有入站上下文**——于是不管运营给两个实例配了什么作息，
后台一律读全局值。现在改成按 `chat_key` 查到频道的 `instance_key`、显式调
`resolve_scoped_config` 解析，配置和快照都取这个实例的那一份。

上游是单实例、也没有 `scope.py`，能力探测不到时整段回落到全局配置，行为与改动前一致。

## 工作流程

### 正常睡眠

```
23:00 → 静默入睡（不发消息）
       ↓ 夜间普通消息：入库但不触发 LLM（strict 模式下完全不入库）
06:45-08:30 → 随机时间自然醒 → 【{persona}已起床：昨日睡眠质量 N%，睡眠时长 X】
```

### 叫醒流程

```
用户有效呼叫 → 【{persona}已经睡了 要叫醒{persona}吗？】(不经 LLM，preserve 模式下进历史)
       ↓ 被问的那个人的下一条消息（不论说了什么）
       → FORCE_TRIGGER 进 LLM，注入告诉它「这条是对方的回答，先判断要不要叫醒你」
            ├─ 对方确实要叫醒 → 正常回复，Bot 醒着（还带睡意）
            └─ 对方并不需要   → 调用 resume_sleep，**一个字都不发**，静默睡回去；
                              这次呼叫降级成「没理会的呼叫」，睡眠段也不算断开
       ↓ 10 分钟没人再找它 → 静默睡回（群里别人自顾自聊天不算「有人找」）
       ↓ 聊完之后 Bot 再调用 resume_sleep → 【{persona}已睡下】
```

**判断交给 LLM，插件不做关键词匹配。**「要」「算了你睡吧」「我要睡了」「你在哪」
一律进同一条路：模型带着上下文读这句话，比任何词表都准。
手写词表试过一版，`要`/`好`/`对` 在中文里太常见，「我要睡了」会被读成同意、
「对不起吵到你了」也会——这类误判无法靠加词修好。

唯一保留的关键词通道是**紧急词直通**（默认关闭）：开启后消息里出现这些词、
且是冲着 Bot 来的，就跳过提问一步叫醒。

被忽略的呼叫照常记进当晚记录并扣质量分——限流只是不再回话，不是当没发生过。

距计划起床 60 分钟内，提示语改为 **【{persona}还没起床 要叫醒{persona}吗？】**。

### 夜间定时任务

默认 `NIGHT_TIMER_POLICY=run`：睡眠期间的系统消息照常放行。宿主只会为
`trigger_agent=True` 的调用方（也就是定时器）真正开一轮，所以普通系统通知仍然不会吵醒它。
这一轮的提示注入会告诉 Bot：

```
[睡眠状态] 你从 23:00 起一直在睡觉，计划 07:24 自然醒。现在是夜里值班——
有个定时任务把你临时叫起来处理，处理完就继续睡，不用寒暄，也不要表现得像已经起床了。
```

值班不会打断睡眠段（定时器不等于起床），但会按「夜间值班计入时长」记账，
其中一半算作损失的休息，总量封顶为整夜的四分之一。
`block` 策略则回到「只入历史、不执行」。

### 重启

启动时枚举「已有睡眠状态的频道」∪「近 N 天有消息的频道」，逐个对账：

- 睡着且已过计划起床点 → 立刻结算；在宽限期内补发播报，超时则静默结算
- 醒着但当前正处于夜间窗口 → 入睡，且**睡眠段回填到真实就寝点**（不是启动时刻）
- 之后维护循环覆盖所有装载过的频道，不再依赖「有人先说话」

### 睡眠质量

**基准是当晚自己的计划**，不是某个固定时长。起床点本来就是在范围内随机抽的，
拿固定值当基准会让「今晚碰巧抽到 6:50 起」被算成睡得差——那是插件自己掷的骰子，
不是这一夜的质量。所以睡满计划 = 100%，超过 100% 来自「整夜没被打扰」的加分。

```
base    = 100 × min(有效睡眠 / 计划睡眠, 1.25)     有效睡眠 = 实际睡眠 − 0.5 × 夜间值班
       − 6 × (睡眠段数 − 1)                       碎片化，封顶 24
       − 4 × w(t)  每次没被理会的呼叫
       − 10 × w(t) 每次真的把人叫起来的呼叫
       + 3          整夜无人打扰、无中断、无值班时的加分
       + 稳定扰动
w(t)    = 0.35 + 0.65 × sin(π × 进度)^1.2         倒 U：刚睡下与快起床时最便宜，后半夜最贵
```

默认配置下的分数（与随机起床点无关）：

| 场景 | 分数 |
|---|---|
| 一夜无扰 | 101–108 |
| 1 次没理会的呼叫 | 94–101 |
| 3 次没理会的呼叫 | 86–94 |
| 6 次没理会的呼叫 | 75–84 |
| 被叫醒 30 分钟 | 76–85 |
| 被叫醒 2 小时 | 58–67 |
| 只睡了 1 小时 | 20–26 |

想按固定时长评分（例如「不管计划多久，8 小时才算 100%」）就把
**目标睡眠时长**设成非 0。

每次结算都会把 `base / 各项扣分 / 扰动 / 原始分 / 目标时长 / 有效时长` 一起存进
`cycle.quality_breakdown`，分数不对时可以直接看是哪一项，而不用反推。

## 拟人化细节

四个都默认不打扰、可单独开关：

- **睡前预告**（默认关）：入睡前 N 分钟发一句【{persona}有点困了，准备去睡了…】，
  比 23:00 突然不理人自然。只对「最近还有人说话」的频道发，免得对着空群自言自语；
  刚装完插件时这个「最近」还没有数据，会回落去查一次该频道的历史消息，
  否则头一两天会一条都不发、看起来像坏了。
- **赖床**（默认 30 分钟）：自然醒之后这段时间，提示词会告诉 Bot 它刚起床、还没完全清醒，
  说话可以慢半拍。不发消息、不多花 token，纯提示词。
- **说梦话**（默认关）：起床播报之后再让 LLM 顺口说一句昨晚梦到了什么。
  **会多跑一轮 LLM**，所以默认关；播报本身被策略跳过时也不会触发。
- **周末晚睡**（默认关）：以**就寝当天**计星期，`4,5` 表示周五、周六晚上换一套作息。
  只填了入睡时间就只换入睡时间，起床范围沿用平日。

## 状态机

```
AWAKE ──(到达入睡点)──→ ASLEEP
                          │
              ┌───────────┤
              │           │
     (LLM 判定要叫醒)  (计划起床)
              │           │
              ▼           ▼
        AWAKE_EARLY ──→ AWAKE
              │
   (空闲 / resume_sleep / LLM 判定不需要)
              │
              ▼
           ASLEEP
```

## 开发

```bash
python -m pytest tests -q
```

`tests/hoststub.py` 伪造 `nekro_agent.*`，让接线层（`__init__.py`）可以脱离宿主被测试。
`tests/test_host_contract.py` 用 `ast` 解析真实 NekroAgent 源码校验这些假设没有漂移
（`db_chat_channel` 仍是普通 property、`MsgSignal` 取值不变、`BLOCK_ALL` 仍早于消息落库等）。
它默认找同级目录 `NekroAgent_ByAkiyo` / `nekro-agent`，也可以显式指定；两个版本都应通过：

```bash
NEKRO_AGENT_SRC=/path/to/nekro-agent python -m pytest tests/test_host_contract.py -q
```

`tools/integration_probe.py` 是**上机器用的**：放进 NekroAgent 根目录、用它的 venv 跑，
会连真实数据库、真实 `message_service`、真实人设跑一遍全链路（启动对账、包装闸门、
叫醒协议、结算播报、重启恢复、`/sleep` 指令），但**一条消息都不会发出去**——
所有出站路径在插件被加载之前就被换成了捕获，唯一被驱动的频道是它自己建、跑完自己删的。

```bash
cp tools/integration_probe.py /opt/NekroAgent_ByAkiyo/
cd /opt/NekroAgent_ByAkiyo && uv run python integration_probe.py
```

上面「配置」那张表由 `tools/gen_config_table.py` 从 `SleepConfig` 生成，改了配置项之后跑：

```bash
python tools/gen_config_table.py --write
```

`tests/test_docs.py` 会在表和代码漂开时报错——这张表原本是手写的，
后来三分之一的配置项没写进去、还有两个默认值是错的。

## 已知未完项

- **夜间值班时长是估算值**：插件拿不到 Agent 轮次的真实耗时（要拿只能猴补丁宿主私有方法），
  所以按「夜间值班计入时长」记一个保守估计，总量封顶整夜的四分之一。
- **`defer` 策略未做**：推迟到起床后执行需要回写定时器队列，属于宿主的活，
  当前只有 `run` 与 `block` 两个诚实的选项。
- **不保留历史评分序列**：`quality_breakdown` 只挂在当前 cycle 上，新的一夜就覆盖。
  这是有意的——这个插件是为了拟人化好玩，不是为了做睡眠数据分析。

## 技术要点

- 不修改任何 NekroAgent 核心文件
- 使用 `PluginStore` 持久化，不建表；启动枚举直接查 `DBPluginData` / `DBChatMessage`
- 只包装宿主的一个**公开**方法 `schedule_agent_task`（且仅在 `block` 策略下真正拦截），
  不再碰任何私有方法
- 每个 `chat_key` 独立状态、独立异步锁
- 领域层（`models` / `schedule` / `engine` / `quality`）不导入宿主单例，可独立测试
- 沙盒方法的**函数名就是模型要调的名字**（宿主渲染的是 `func.__name__`，
  `mount_sandbox_method` 的 name 参数只是给人看的显示名），所以工具函数直接叫
  `resume_sleep`；提示注入里的名字也从函数本身取，不写死字符串
- `resume_sleep` 通过 `mount_collect_methods` 按状态收集：只有被提前叫醒、且还没到
  计划起床点时才出现在提示词里，白天一个 token 都不占（常驻插件本轮不交方法时，
  宿主会把整块声明一起收掉）
- **不使用宿主的插件休眠**（`allow_sleep=False`）：休眠态只渲染简介、不渲染方法，
  模型得先调 `extend_plugin_activation` 才拿得到 `resume_sleep`——而这个工具恰恰
  只在「有人让它去睡」那一刻才有用，模型会答应下来却没有工具可用。省 token 这件事
  `collect_methods` 已经做了，不需要再叠一层休眠
- 命令系统缺失时（老版本宿主）自动跳过 `/sleep` 注册，插件仍可加载
- 每频道解析出的作息与实例配置带 300 秒 TTL 缓存，写入覆盖时显式失效
- 插件日志经 `_HostLogBridge` 转发进宿主 logger：领域层保持只依赖 stdlib `logging`
  （不 import 宿主），但线上能在宿主日志里按 `plugin.<key>` 过滤到它
- 状态落库时盖当前 `SCHEMA_VERSION`，避免迁移过的行一直自称旧版本
- 通过运行时 `getattr` 能力探测兼容两个版本，不按版本号分支

## 许可

与 NekroAgent 保持一致。
